<form class="mt-4" method="post" action="/courses/{{course._id}}?_method=PUT">
  <div class="form-group">
    <label for="=">Tên khóa học</label>
    <input name="name" class="form-control" placeholder="Nhập tên khóa học" value="{{course.name}}">
  </div>
  <div class="form-group">
    <label for="=">Mô tả khóa học</label>
    <input name="describe" class="form-control" placeholder="Nhập mô tả khóa học" value="{{course.describe}}">
  </div>
  <div class="form-group">
    <label for="=">Video</label>
    <input name="videoId" class="form-control" placeholder="Nhập mã video" value="{{course.videoId}}">
  </div>
    <div class="form-group">
    <label for="=">Trình độ</label>
    <input name="level" class="form-control" placeholder="Nhập tên slug" value="{{course.level}}">
  </div>
  <button type="submit" class="btn btn-primary">Lưu lại</button>
</form>